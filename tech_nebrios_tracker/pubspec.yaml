name: tech_nebrios_tracker
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  intl: ^0.18.1
  
  http: ^1.1.0 # Comunicación HTTP
  dio: ^5.3.2  # Cliente HTTP más avanzado
  cached_network_image: ^3.2.3 # Cargar imágenes
  path_provider: ^2.1.5
  path: ^1.8.3
  open_file: ^3.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
